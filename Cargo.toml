[package]
name = "seasonal-tree"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
hex_color = { version = "3", features = ["serde"] }
rand = "0.10"
serde = { version = "1.0", features = ["derive"] }
tiny-skia = "0.12.0"
toml = "1.1.4"
